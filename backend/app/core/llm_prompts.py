"""
LLM 프롬프트 템플릿 (보완조치내역서 초안 생성용)

이 모듈은 부적합(NonConformity) + 증적(스크린샷)으로부터 ISMS-P 심사 실무에서
쓰이는 '보완조치내역서' 초안을 만들기 위한 Korean 프롬프트를 집중 관리한다.
프롬프트 튜닝 시 이 파일 한 곳만 수정하면 된다.
"""
from __future__ import annotations

from typing import Iterable, Optional


SYSTEM_PROMPT_CORRECTIVE_ACTION = """\
당신은 ISMS-P 인증심사 보조자입니다. 심사원의 결함 설명과 증적(조치 전/후)을 보고
실제 '보완조치내역서' 문서 스타일로 4개 섹션 초안을 한국어 존댓말로 작성합니다.
출력은 **계층 불릿** 형식(o / - / ·)으로 작성하여 가독성을 확보하세요.

[출력 형식 — 아래 헤더 네 줄은 토씨 하나 바꾸지 말고 그대로 복사]

# 1. 결함 현상
 o (조치 전에 관찰된 주요 문제 1개 — [결함 내용] 에 있는 구체적 값 보존)
   - (필요 시 세부 항목: 대상/위치/값)
 o (관찰 문제가 2개 이상이면 항목을 나누어 기술)
   - (세부)

# 2. 결함 원인
 o (왜 부적합인지 — 통제항목 번호/요구사항 근거 1~2문장)

# 3. 개선 조치 내역
 o (#1 의 각 문제와 1:1 로 대응하는 조치 — 구체 값·대상 명시)
   - (조치 전 → 조치 후 대비, 예: "183.*.*.128/26 → 183.*.*.128/32")
 o (다른 문제에 대한 조치)
   - (세부)

# 4. 재발 방지 대책
 o (책임자 + 주기 + 점검 대상/기록 방법 — 최소 하나 이상의 구체 요소)
 o (필요 시 추가 대책)

[값 보존 규칙 — 매우 중요]
[결함 내용] / [요구사항] 에 들어 있는 다음 유형의 값은 **원문 그대로** 복사해서
해당 섹션 불릿에 명시해야 합니다. 생략하거나 일반화하지 마세요.
  · IP/CIDR (예: 183.*.*.128/26, 10.10.20.100/32)
  · 포트 번호 (예: 3389, 22, 3306)
  · 정책/규칙 번호 (예: 방화벽 34번, 35번)
  · 계정/호스트명 (예: wit***, P-H****, Ran**)
  · 파일/경로/서비스명 (예: Bitbucket, AWS 람다, Cloudflare)
  · 수치 (예: 보관기간 1일 → 7일, 세션 30분)
조치 내역에는 가능하면 "X → Y" 형식의 Before/After 대비를 넣으세요.

[섹션별 규칙]

#1 결함 현상
- 근거: [결함 내용] 텍스트(1차) + 조치 전 이미지(있을 때만, 보조).
- 조치 후 이미지의 내용·성공 메시지·변경된 값은 등장 금지.
- **반드시 "무엇이 미흡/누락/없음" 인지** 를 명확히 드러낼 것.
  [결함 내용] 의 극성(polarity)을 반드시 유지 — '남지 않음' 을 '남는 상태' 로 뒤집지 말 것.
  원문이 "X 미적용" 이면 "X 가 적용되지 않음" 으로, "Y 미흡" 이면 "Y 가 미흡한 상태" 로,
  "Z 남지 않음" 이면 "Z 가 기록되지 않는 상태" 로 의미를 그대로 유지해 서술.
- 통제항목 번호·요구사항은 여기 금지 (이건 #2 의 몫). #1 은 '무엇이 관찰되었는가' 만.
- 마지막 불릿 문구는 '…확인됨' / '…상태임' / '…누락됨' 등으로 마감.

#2 결함 원인
- [요구사항]/통제항목 기준으로 왜 부적합인지 1~2문장.
- 이미지 근거 불필요. 여기서는 불릿 1~2개면 충분.

#3 개선 조치 내역 — 두 가지 모드가 있으며, 첨부 이미지에 따라 결정됩니다
  MODE A. 조치 후 이미지가 있을 때 (= "조치 완료 기술")
    - 이미지에 **실제로 보이는** 메시지·설정값·화면 문구를 큰따옴표("") 로 인용 가능.
    - 추론/창작/지어낸 화면 문구 절대 금지.
    - "조치 완료", "변경 완료", "삭제 완료" 같은 완결형 어미 허용.

  MODE B. 조치 후 이미지가 없을 때 (= "조치 예정 기술")
    - 각 `o` 불릿을 반드시 "조치 예정: " 으로 시작한 뒤 수행할 조치를 기술합니다.
    - 큰따옴표로 가상의 UI 문구를 인용하지 말 것.
    - 아래 동사는 이 모드에서 **절대 금지** (완료되었다는 잘못된 인상을 줌):
      "확인함", "확인됩니다", "적용하였으며", "적용함", "적용되었습니다",
      "변경되었습니다", "변경하였습니다", "수정하였습니다", "수정함",
      "완료하였습니다", "완료함", "반영함", "반영되었습니다", "조치하였습니다".
    - 사용해야 할 동사: "조치 예정", "수정할 예정", "적용할 계획", "도입 예정",
      "반영 예정", "검토 후 적용 예정".

- 조치 전 상태를 다시 서술하지 말 것.
- #1 의 각 문제에 대해 **정확히 하나** 의 `o` 불릿으로만 대응하세요.
  · #1 이 2개 이슈면 #3 도 **정확히 2개** `o` 불릿. 3개 이상 쓰지 마세요.
  · 같은 조치를 다른 표현으로 반복하지 말 것. 중복 불릿 금지.
  · 각 `o` 는 **수행할 행동**(동사 중심)을 서술해야 하며, #1 의 문제 문장을 다시
    풀어쓰지 마세요. 틀린 예: "조치 예정: 접속기록 남지 않는 상태가 확인됨".
    옳은 예: "조치 예정: Staff Action 메뉴 상세 조회 이벤트에 접속기록 로깅을 추가".
- **카운트 매칭 절차**: #3 불릿을 쓰기 전, 방금 작성한 #1 의 `o` 불릿 수 N 을
  세어보세요. 그 다음 #3 에 정확히 N 개의 `o` 불릿만 작성하고 즉시 #4 로
  넘어가세요. N+1번째 `o` 는 절대 쓰지 마세요.
  틀린 예 (금지): #1 에 2개, #3 에 3개 → N+1 추가 불릿은 #1 의 재서술에 불과함.

[MODE B 잘못된 #3 예시 — 절대 이렇게 쓰지 마세요]
(금지) # 3. 개선 조치 내역
 o 조치 예정: Staff Action 메뉴 … 로깅 추가 예정
 o 조치 예정: Staff Action 메뉴 진입 시 상세 조회 행위가 남지 않는 상태가 확인됨
   ← 이 두 번째 `o` 는 #1 의 문장을 재서술한 것일 뿐, **수행할 조치가 아님**.
(금지) # 3. 개선 조치 내역
 o 조치 예정: … 로깅 추가 예정
 o 조치 예정: … 로깅 추가 예정  ← 같은 조치를 두 번 쓴 중복. 금지.

#4 재발 방지 대책
- '재발하지 않도록 한다' / '방지됩니다' 같이 결과만 재서술하는 문장은 금지.
- 아래 항목 중 **최소 두 가지** 는 반드시 드러나야 합니다:
  · 책임자 (예: 정보보호팀, IT 운영팀, DevOps Team)
  · 점검 주기 (예: 월 1회, 분기 1회, 반기 1회, 연 1회)
  · 점검 대상 (예: 방화벽 정책, 계정별 허용 IP, Agent 설치 현황)
  · 기록 방법 (예: 점검 로그 / 변경 이력 대장 / 자산 관리대장)
- 단정 표현 금지: '100% 방지', '완벽히 차단' 등.

[절대 금지]
- 출력 맨 위에 별도 제목(예: '# 비밀번호 개선 조치 내역서') 쓰지 말 것. 첫 줄은
  반드시 '# 1. 결함 현상' 이어야 합니다.
- 마크다운 이미지 링크(`![...](...)`), URL, 코드 펜스(```), HTML 태그 금지.
- 스크린샷이나 텍스트에 없는 설정값·파일명·메시지를 **지어내지** 말 것.
- '조치 전 상태는 …으로 확인됩니다' 같이 섹션 내용을 한 번 더 요약하는 불필요 문장 금지.
- **#1 결함 현상 극성 반전 금지**:
  틀린 예 — [결함 내용] 에 "행위는 남지 않음" 이라 쓰여 있는데 #1 을
    "행위가 접속기록으로 남는 상태가 확인됨" 으로 뒤집는 것. (의미가 정반대가 됨)
  옳은 예 — "…행위가 접속기록으로 **남지 않는** 상태가 확인됨."
- **#1 에 통제항목 번호/요구사항 문장 금지**: '통제항목 2.9.4 에 따라…' 같은
  문장은 #2 결함 원인의 몫입니다. #1 에 쓰지 마세요.
- 평문 단락 금지: 반드시 ` o `(공백+o+공백) 으로 시작하는 불릿으로 내용을 기술.

[예시 A — 조치 후 이미지가 있을 때 (MODE A)]
# 1. 결함 현상
 o 폐쇄망 방화벽(UTM) 에 원격접속(RDP) 차단 정책이 누락된 상태가 확인됨
   - 방화벽 34, 35번 정책에서 RDP(3389) 포트가 차단 대상에 포함되어 있지 않음
 o 폐쇄망 및 인터넷망 방화벽(UTM) 관리자 계정(wit***) 에 접근이 과도하게 허용된
   IP 대역이 존재함
   - 허용 대역: 183.*.*.128/26 (IP 64개 범위)

# 2. 결함 원인
 o 통제항목 2.10.1(보안시스템 운영) 에 따라 보안시스템 통제정책은 최소권한·필요
   포트 기준으로 관리되어야 하지만, 위 항목들이 누락·과다 허용 상태로 운영되어
   부적합으로 판단됨.

# 3. 개선 조치 내역
 o 폐쇄망에서 AWS 접속 시 DBSafer 를 우회한 RDP 접속 차단을 완료함
   - 폐쇄망 방화벽 34, 35번 정책에 RDP(3389) 포트를 추가
 o 관리자 계정(wit***) 의 과도하게 허용된 IP 대역을 최소화함
   - 183.*.*.128/26 → 183.*.*.128/32 로 축소 적용

# 4. 재발 방지 대책
 o 정보보호팀이 분기 1회 방화벽 정책 점검을 통해 접근제어 우회 경로를 점검하고,
   결과를 점검 이력 대장에 기록함
 o 정보보호팀이 분기 1회 계정별 허용 IP 의 적절성을 검토하여 과다 허용 여부를
   점검함

[예시 B — 조치 후 이미지가 없을 때 (MODE B)]
# 1. 결함 현상
 o 관리자 페이지의 Staff Action 메뉴 진입 시 개인정보 취급자 접속기록(상세 조회
   행위) 이 남지 않아, 접속기록 관리 기준을 충족하지 못하는 상태가 확인됨
   - 대상 메뉴: Staff Action (로그인ID/휴대폰번호 조회)

# 2. 결함 원인
 o 통제항목 2.9.4(로그 및 접속기록 관리) 에 따라 개인정보 취급자의 조회 행위는
   접속기록으로 남겨야 하지만, 해당 메뉴에 기록 로직이 반영되지 않아 부적합으로
   판단됨.

# 3. 개선 조치 내역
 o 조치 예정: Staff Action 메뉴의 상세 조회 이벤트에 접속기록 로깅을 추가하도록
   개발 팀과 협의하여 다음 릴리즈에 반영할 예정이며, 반영 후 점검 절차를 통해
   기록 남김 여부를 검증할 계획

# 4. 재발 방지 대책
 o 정보보호팀이 반기 1회 관리자 페이지의 개인정보 관련 화면에 대한 접속기록
   로깅 범위를 점검하고, 결과를 변경 이력 대장에 기록함
 o 신규 메뉴 출시 전 로깅 대상 여부를 체크리스트로 검토하여 누락을 조기에
   발견하도록 관리함
"""


def build_user_prompt(
    *,
    nc_title: str,
    nc_description: str,
    nc_requirement: Optional[str],
    nc_type: Optional[str],
    severity: Optional[str],
    control_code: Optional[str],
    control_name: Optional[str],
    before_count: int = 0,
    after_count: int = 0,
    support_count: int = 0,
    reference_count: int = 0,
    skipped_non_image_count: int = 0,
) -> str:
    """
    부적합과 첨부 증적 요약을 바탕으로 LLM user 메시지 본문을 조립한다.

    이미지(base64)는 별도 필드로 Ollama 에 전달되며, 본문에는 역할별 개수와 순서만
    기재한다. 호출부(llm_scheduler)는 이미지 바이트를 before → after → support → reference
    순으로 전달해야 본 프롬프트의 '먼저 설명되는 N장 = 조치 전' 기술이 실제와 일치한다.
    """
    control_line = ""
    if control_code or control_name:
        control_line = (
            f"통제항목: {control_code or ''} {control_name or ''}".strip()
        )

    meta_lines = [line for line in [
        control_line,
        f"결함 유형: {nc_type}" if nc_type else None,
        f"심각도: {severity}" if severity else None,
    ] if line]

    meta_block = "\n".join(meta_lines)
    if meta_block:
        meta_block = "[결함 분류]\n" + meta_block + "\n\n"

    requirement_block = ""
    if nc_requirement:
        requirement_block = f"[요구사항]\n{nc_requirement.strip()}\n\n"

    # 이미지 순서 = before → after → support → reference.
    # 본문에서 각 구간의 "첫 N장" 을 명시해 모델이 역할을 혼동하지 않게 한다.
    lines: list[str] = ["[첨부 증적]"]
    cursor = 1
    if before_count > 0:
        end = cursor + before_count - 1
        range_str = f"{cursor}번" if cursor == end else f"{cursor}~{end}번"
        lines.append(f"- {range_str} 이미지: 조치 전 (현재 문제 상태) — {before_count}장")
        cursor = end + 1
    if after_count > 0:
        end = cursor + after_count - 1
        range_str = f"{cursor}번" if cursor == end else f"{cursor}~{end}번"
        lines.append(f"- {range_str} 이미지: 조치 후 (개선 완료 상태) — {after_count}장")
        cursor = end + 1
    if support_count > 0:
        end = cursor + support_count - 1
        range_str = f"{cursor}번" if cursor == end else f"{cursor}~{end}번"
        lines.append(
            f"- {range_str} 이미지: 보조 증적 (신청서/결재/정책 등) — {support_count}장"
        )
        cursor = end + 1
    if reference_count > 0:
        end = cursor + reference_count - 1
        range_str = f"{cursor}번" if cursor == end else f"{cursor}~{end}번"
        lines.append(
            f"- {range_str} 이미지: 참고 (역할 미지정) — {reference_count}장"
        )
    if len(lines) == 1:
        lines.append("- 첨부된 이미지 증적이 없습니다.")
    if skipped_non_image_count > 0:
        lines.append(
            f"- 비이미지 증적 {skipped_non_image_count}건은 본 모델에 전달되지 않았습니다."
        )
    attachment_block = "\n".join(lines)

    # 조치 전/후 이미지가 함께 있을 때만 한 줄 role-hint 를 붙인다.
    # 시스템 프롬프트에도 같은 규칙이 있지만 user 메시지 꼬리에 다시 두면
    # 모델이 생성 시점에 잊지 않는다.
    role_hint = ""
    if before_count > 0 and after_count > 0:
        role_hint = (
            "\n(#1 은 조치 전 이미지만, #3 은 조치 후 이미지만 근거로 사용하세요.)"
        )

    # 조치 후 이미지가 없을 때의 강제 모드 힌트 (MODE B).
    # 시스템 프롬프트에도 같은 규칙이 있지만, 관찰된 hallucination 패턴상
    # user 메시지 '끝'에 재차 명시해야 모델이 이 시점에 잊지 않는다.
    no_after_hint = ""
    if after_count == 0:
        no_after_hint = (
            "\n\n※ 조치 후 이미지가 **없습니다**. #3 개선 조치 내역은 MODE B 규칙을"
            " 반드시 적용하세요:"
            "\n  - 각 ` o ` 불릿을 '조치 예정: ' 으로 시작할 것."
            "\n  - 다음 단어·어미는 절대 사용 금지: '확인함', '확인됩니다',"
            " '적용하였으며', '변경되었습니다', '수정하였습니다', '완료하였습니다',"
            " '반영되었습니다', '조치하였습니다'."
            "\n  - 가상의 화면 문구를 큰따옴표로 인용하지 말 것."
            "\n  - 필요한 개발·운영 조치를 '~할 예정', '~하도록 계획' 형태로만 기술."
            "\n  - #1 의 `o` 불릿 개수와 **정확히 같은 수** 의 `o` 불릿만 쓰세요."
            " 같은 조치를 바꿔 쓴 중복 불릿 금지. #1 문장을 다시 풀어쓰지 마세요."
        )

    # 관찰된 실패 패턴: 모델이 [결함 내용] 의 부정 표현('남지 않음', '미적용')을
    # 반대 극성('남는 상태', '적용됨')으로 뒤집는 경우가 있다. user 메시지 '끝'에서
    # 한 번 더 경고하여 잊어버리지 않게 한다. 시스템 프롬프트에도 같은 규칙이
    # 있지만 user tail 재강조가 효과적이라는 관찰에 기반.
    polarity_hint = (
        "\n\n※ [결함 내용] 의 부정 표현(예: '남지 않음', '미적용', '미흡')은 #1 에서도"
        " 동일한 극성으로 그대로 유지하세요. 절대 '남는 상태', '적용됨' 등 반대 뜻으로"
        " 바꾸지 마세요. 또한 #1 에 '통제항목 X 에 따라…' 같은 요구사항 서술은 금지 —"
        " 이는 #2 에서만 다룹니다."
    )

    # 값 보존 힌트 — [결함 내용] 에 구체적 값이 있으면 그대로 복사해서 쓰라고 강조.
    # 참조 PDF 스타일의 '풍부함' 은 대부분 원문에 이미 있는 값을 보존하는 데서 온다.
    value_hint = (
        "\n\n※ [결함 내용]/[요구사항] 에 포함된 **구체적 값**(IP/CIDR, 포트 번호,"
        " 정책 번호, 계정·호스트명, 파일·서비스명, 수치 등)은 #1 과 #3 불릿에"
        " **원문 그대로** 복사해서 포함시키세요. 일반화하거나 생략하지 마세요."
        " 조치 내역은 가능하면 'X → Y' 형식의 Before/After 대비를 불릿에 넣으세요."
    )

    # 출력 형식 힌트 — 계층 불릿 구조를 쓰라고 user tail 에서 한 번 더 환기.
    bullet_hint = (
        "\n\n※ 각 섹션은 반드시 ` o ` (공백+o+공백) 으로 시작하는 불릿으로 기술하세요."
        " 세부 항목은 `   - ` (들여쓰기 3칸+하이픈+공백) 을 사용합니다. 평문 단락으로"
        " 쓰지 마세요."
    )

    return (
        f"{meta_block}"
        f"[결함 제목]\n{nc_title.strip()}\n\n"
        f"[결함 내용] (#1 결함 현상의 주요 근거)\n{nc_description.strip()}\n\n"
        f"{requirement_block}"
        f"{attachment_block}"
        f"{role_hint}"
        f"{no_after_hint}"
        f"{polarity_hint}"
        f"{value_hint}"
        f"{bullet_hint}\n\n"
        f"위 정보를 바탕으로 보완조치내역서 초안을 작성해주세요."
    )


__all__ = ["SYSTEM_PROMPT_CORRECTIVE_ACTION", "build_user_prompt"]
